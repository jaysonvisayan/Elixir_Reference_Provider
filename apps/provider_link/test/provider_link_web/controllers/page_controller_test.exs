defmodule ProviderLinkWeb.PageControllerTest do
  # use ProviderLinkWeb.ConnCase
  # import ProviderLinkWeb.TestHelper

  # setup do
  #   conn = build_conn()
  #   user = insert(:user)
  #   conn = sign_in(conn, user)
  #   {:ok, %{conn: conn, user: user}}
  # end

  # test "GET /", %{conn: conn} do
  #   conn = get conn, "/"
  #   assert html_response(conn, 200) =~ "Payor"
  # end

end
