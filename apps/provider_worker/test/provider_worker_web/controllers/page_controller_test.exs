defmodule ProviderWorkerWeb.PageControllerTest do
  use ProviderWorkerWeb.ConnCase

  #TODO: apply fixes for new conn

  # setup do
  #   conn = build_conn()
  #   user = insert(:user)
  #   conn = authenticated(conn, user)
  #   {:ok, %{conn: conn}}
  # end

  # test "GET /", %{conn: conn} do
  #   raise conn
  #   conn = get conn, "/"
  #   assert html_response(conn, 200) =~ "Welcome to Phoenix!"
  # end
end
